spring:
  shell:
    interactive:
      enabled: false

  datasource:
    url: jdbc:h2:mem:library;DB_CLOSE_DELAY=-1
    initialization-mode: always
    data: data.sql
    driverClassName: org.h2.Driver